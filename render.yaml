services:
  - type: web
    name: footprint-logger-api
    env: docker
    plan: starter
    dockerfilePath: ./backend/Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGO_URL
        value: ${MONGO_URL}
